#!/bin/sh
set -eu

systemctl --user start easyeffects.service

attempt=1
while [ "$attempt" -le 10 ]; do
  if easyeffects --bypass 1 >/dev/null 2>&1; then
    exit 0
  fi

  sleep 0.5
  attempt=$((attempt + 1))
done

exit 1
